# Main Caddy config for a multi-site VPS.
# On the server: copy to /etc/caddy/Caddyfile
#
#   sudo cp deploy/caddy/Caddyfile /etc/caddy/Caddyfile
#   sudo cp deploy/caddy/sites/*.caddy /etc/caddy/sites/
#   sudo systemctl reload caddy
#
# Optional: Let's Encrypt expiry notices
# {
#     email you@example.com
# }

import sites/*.caddy
